export const IntroEx = ({ name, age }) => {
  //     console.log(props);
  //   const name = props.name;
  //   const age = props.age;

  //   const { name, age } = props;
  //   console.log(name, age);

  return (
    <div>
      <h2>이름: {name}</h2>
      <h2>나이: {age}</h2>
    </div>
  );
};
